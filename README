I am creating a Chrome Extension that replaces the new tab page.

It allows the user to add bookmarks to any website.

This can be done manually through the new tab page or via a toolbar icon from the actual page
